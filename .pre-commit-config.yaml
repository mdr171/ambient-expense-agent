repos:
  - repo: local
    hooks:
      - id: semgrep
        name: Semgrep Security Scan
        entry: semgrep scan --config=.semgrep/rules.yaml --error
        language: system
        types: [python]
